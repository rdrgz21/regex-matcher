import { ReactElement } from "react";
import styles from "./DocumentPreview.module.css";

export const DocumentPreview = (): ReactElement => {
  return <div className={styles.documentPreview}>DocumentPreview</div>;
};
